BRA_electoral_system = {

	BRA_next_election = {

		icon = generic_decision

		available = {
			OR = { 
			NOT = { has_country_flag = BRA_REPUBLIC }
			has_country_flag = BRA_VARGAS_DEATH
			}
		}

		activation = {
			tag = BRA
		}

		is_good = yes

		days_mission_timeout = 900
		fire_only_once = yes

          timeout_effect = {                  
            country_event = { id = twrbrazil.12 days = 0 }
			set_country_flag = BRA_ELECTION_1956
          }		
		complete_effect = { }
		ai_will_do = {
			base = 100
		}

}

	BRA_next_election_2 = {

		icon = generic_decision

		available = {
OR = { 
NOT = { has_country_flag = BRA_REPUBLIC }
has_country_flag = BRA_VARGAS_DEATH
}
		}

		activation = {
		tag = BRA
		has_country_flag = BRA_ELECTION_1956
		}

		is_good = yes

		days_mission_timeout = 1200

          timeout_effect = {                  
             country_event = { id = twrbrazil.13 days = 0 }
set_country_flag = BRA_ELECTION_1960
          }		
complete_effect = { }
		ai_will_do = {
			base = 0
		}
	}
}