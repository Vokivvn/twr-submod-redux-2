RSB_karbyshevs_health_category = {

	picture = GFX_decision_cat_rmo_nordic_settlers
	allowed = {
		tag = RSB
	}

	visible = {
		OR = {
			has_war_with = SMO
			has_war_with = RMD
			has_war_with = RMO
		}
	}
	visible_when_empty = yes

	priority = 1000
}