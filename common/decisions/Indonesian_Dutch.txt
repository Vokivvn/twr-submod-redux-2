operations = {
	
	retreat_from_indies = {

		icon = generic_ignite_civil_war

		allowed = {
			tag = HOL
		}

		cost = freedec?0

		available = {
		has_war_with = INS
		NOT = {
		controls_state = 850
			}
		}

		visible = {
		has_war_with = INS
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision retreat_from_indies"
			add_stability = 0.15
			country_event = {
                 id = TWR_HOL.2000 days = 1
            }
			set_country_flag = IN_PEACE_PROCESS_HOL_INS
		}

		ai_will_do = {
			factor = 50
		}
	}
}
