in_war_with_ta = {
	is_in_faction_with = USA
	OR = {
		has_war_with = event_target:ta_defeated_by
		AND = {
			is_subject = yes
			overlord = {
				has_war_with = event_target:ta_defeated_by
			}
		}
	}
}